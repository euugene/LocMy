import React from "react";
import classes from './Messages.module.css';

const Logo = () => {
    return (
        <div className={classes.messages}>
        Messages
        </div>
    );
}

export default Logo;