import React from "react";
import classes from './Logo.module.css';

const Logo = () => {
    return (
        <div className={classes.logo}>
            <div>LOGO</div>
        </div>
    );
}

export default Logo;